services:
  - type: web
    name: etl-analytics-dashboard
    runtime: python
    buildCommand: pip install -r scripts/requirements.txt && python scripts/export_data.py
    startCommand: python scripts/dashboard.py
    plan: free
    envVars:
      - key: PORT
        value: 8000
